# ReoGrid Lite — React Example

## Setup

```bash
npm install
npm run dev
```

## Notes

- Requires React 17+
- `@reogrid/lite` must be installed (included in devDependencies here for demo purposes)
- The grid fills the remaining viewport height via `flex: 1`
